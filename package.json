{
  "name": "draw-canvas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "cd client; brunch build",
    "nodemon": "nodemon index.js --ignore 'server/public/**/*'",
    "deploy": "git push heroku master",
    "oldDeploy": "git subtree push --prefix server heroku master",
    "forceDeploy": "git push heroku `git subtree split --prefix server master`:master --force"
  },
  "engines": {
    "node": "4.2.2"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "brunch": "^2.9.1"
  }
}
